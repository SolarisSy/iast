# ===========================================
# VARIÁVEIS DE AMBIENTE - DOCKER
# ===========================================
# Copie este arquivo para .env.docker e preencha com suas credenciais
# 
# Comando: cp env.docker.example .env.docker

# ===========================================
# OPENAI API KEY (OBRIGATÓRIO) ⚠️
# ===========================================
# Obtenha sua chave em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# ===========================================
# PORTAS (OPCIONAL)
# ===========================================
# Altere apenas se as portas padrão estiverem em uso

# Porta do frontend (padrão: 3001)
FRONTEND_PORT=3001

# Porta do backend (padrão: 4000)
BACKEND_PORT=4000

# ===========================================
# FRONTEND (OPCIONAL)
# ===========================================
# URL do backend para produção
# Em desenvolvimento, use: http://localhost:4000
# Em produção, use a URL pública do backend
NEXT_PUBLIC_API_URL=http://localhost:4000

# ===========================================
# AMBIENTE (OPCIONAL)
# ===========================================
# Modo de execução: development | production
NODE_ENV=development

